import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Nenhuma configuração extra é necessária para este projeto.
  // O Vite irá automaticamente usar o 'index.html' como entrada.
});